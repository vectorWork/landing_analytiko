{
  "name": "contactoGrupoSip",
  "version": "1.0.0",
  "description": "contactoGrupoSip",
  "main": "index.js",
  "dependencies": {
    "axios": "^1.6.8",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "nodemon": "^3.1.0"
  },
  "scripts": {
    "start": "nodemon --watch backend --exec server.js",
    "build": "npm install && npm run build",
    "serve": "node server.js",
    "heroku-prebuild": "cd frontend && npm install --dev"
  }
}
